CREATE TABLE usuarioEntity (
    id INTEGER NOT NULL PRIMARY KEY,
    nome TEXT NOT NULL,
    data_bloqueio TEXT
);

insertOrUpdate:
INSERT OR REPLACE INTO usuarioEntity (
    id,
    nome,
    data_bloqueio
) VALUES ( ?, ? , ?);

get:
SELECT * FROM usuarioEntity LIMIT 1;

deleteAll:
DELETE FROM usuarioEntity;